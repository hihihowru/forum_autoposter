import os
from typing import Dict, Any

# Placeholder stub. Replace with real Sheets API integration later.

def append_row_stub(sheet: str, row: Dict[str, Any]) -> None:
    _ = os.getenv("SHEETS_SPREADSHEET_ID")
    # No-op in stub
    return None