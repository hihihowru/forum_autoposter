[build]
builder = "nixpacks"

[deploy]
startCommand = "cd docker-container/finlab python/apps/unified-api && uvicorn main:app --host 0.0.0.0 --port $PORT"
healthcheckPath = "/health"
healthcheckTimeout = 300
restartPolicyType = "on_failure"

[env]
NODE_ENV = "production"
PYTHONPATH = "/app"
