[build]
builder = "nixpacks"

[build.environment]
PYTHON_VERSION = "3.11"
NODE_VERSION = "18.x"

# Backend Service
[[services]]
name = "backend"
port = 8080
workingDirectory = "/"

[services.build]
base_dockerfile = "docker-container/finlab python/apps/dashboard-backend/Dockerfile"

[services.start]
command = "uvicorn src.main:app --host 0.0.0.0 --port 8080"

[services.env]
PYTHONPATH = "/"
DATABASE_URL = { type = "string", required = true }

# Frontend Service
[[services]]
name = "web"
port = 3000

[services.build]
base_dockerfile = "Dockerfile.simple"

[services.start]
command = "serve -s dist -l 3000"

[services.env]
VITE_API_BASE_URL = { type = "string", required = true, value = "/api" }
NODE_ENV = "production"

[deploy]
public = true

